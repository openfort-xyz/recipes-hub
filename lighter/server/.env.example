# Copy this file to .env.local and fill in your values.

# Server
PORT=3008
# Comma-separated list of origins allowed to call this API. Empty = allow all (dev only).
CORS_ORIGINS=

# --- Openfort: Project Keys (Dashboard -> API Keys) ---
OPENFORT_SECRET_KEY=

# --- Openfort: Shield (Dashboard -> Shield > API Keys) ---
# Required for the app's automatic embedded wallet recovery.
SHIELD_PUBLISHABLE_KEY=
SHIELD_SECRET_KEY=
SHIELD_ENCRYPTION_SHARE=

# --- Lighter ---
# Mainnet only. Point this at https://api.rh.lighter.xyz to target Robinhood Chain instead —
# no other code changes needed (see lighter/docs/lighter-signing-notes.md).
LIGHTER_API_BASE_URL=https://mainnet.zklighter.elliot.ai
# L2 signing domain id, NOT an EVM chain id. 304 = mainnet zklighter.
LIGHTER_CHAIN_ID=304
# The single market this recipe trades. 0 = ETH perp on mainnet zklighter.
LIGHTER_MARKET_INDEX=0
LIGHTER_MARKET_SYMBOL=ETH

# Populated after onboarding (see scripts/e2e.md):
# 1. Deposit from the embedded wallet -> Lighter assigns an account_index.
# 2. POST /api/lighter/changepubkey/message + the wallet's personal_sign -> POST .../submit.
# 3. Copy the values the server logs on submit into the three vars below and restart.
LIGHTER_ACCOUNT_INDEX=
LIGHTER_API_KEY_PRIVATE_KEY=
LIGHTER_API_KEY_INDEX=2
